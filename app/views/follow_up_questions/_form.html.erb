<%= form_for(@follow_up_question) do |f| %>
  <% if @follow_up_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@follow_up_question.errors.count, "error") %> prohibited this follow_up_question from being saved:</h2>

      <ul>
      <% @follow_up_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :question, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_area :question, type: "textarea", class: "form-control", placeholder: "Question to ask", rows: "3" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :locale, "Language", class: "col-sm-2 control-label" %>
    <%= f.select :locale, locale_array, class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-success" %>
  </div>
<% end %>
